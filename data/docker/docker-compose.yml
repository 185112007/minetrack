version: '3.5'

services:
  postgres:
    image: 'postgres:latest'
    container_name: pg_minetrack
    environment:
      POSTGRES_DB: minetrack
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - '5432:5432'