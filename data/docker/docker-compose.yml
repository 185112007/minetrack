version: '3.5'

services:
  postgres:
    image: 'postgres:12-bullseye'
    container_name: pg_minetrack
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - '5432:5432'